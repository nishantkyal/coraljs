include scriptsAndStyles
include header

.container-fluid
    .row-fluid
        .col-md-2
        .col-md-8
            .list-group
                each member in members
                    a.list-group-item(href=member.role == 3 ? '/member/' + member.id + '/profile' : '/integration/' + member.integration_id + '/members') #{member.integration.title}
        .col-md-2